# /insights 振り返り（2026-09-25）

Claude Code の `/insights`（2026-08-24〜09-24、134 セッション）を dev-flow の観点で読み直した記録。詳細分析された 50 セッションのうち約 45 件が dev-flow 由来（オーケストレーター、Dev / QA / レビュアー、spec writer）だったので、指摘はほぼそのまま dev-flow の改善点になる。9/22 の振り返り（[2026-09-22-laravel-react-task-app.md](2026-09-22-laravel-react-task-app.md)）で対応済みのもの（pane 型ハングの fork フォールバック、`environment.md`、自動マージ条件）は除いた。

## 指摘と対応

| # | 指摘（/insights の摩擦） | 対応 |
|---|---|---|
| 1 | オーケストレーターが実際の状態を確かめずに報告した（失敗済みの CI を「実行中」、44 コミット遅れのブランチで「全件パス」、存在しないテストを「ある」） | `hooks/verify-remote-state.sh` を新設（fetch・ahead/behind・PR の state・CI の結果を OK / NG で出す）。`dev-flow/SKILL.md` に「状況報告のルール」（報告前に確認し出力を引用）。implementation → test は `state-sync.sh` が確認済みでなければ差し戻す。test ステージに STEP 0.5（origin と同期してからテスト）。`session-start.sh` がブランチの遅れを警告。state.json にトップレベルの `base_branch` |
| 2 | レビュー指摘が一部しか修正担当に転送されなかった | `dev-flow-implementation/SKILL.md` に「レビュー指摘の渡し方」（JSON のまま全件）。implementer は `result.review_responses` で 1 件ずつ回答し、再レビューで照合 |
| 3 | シェル／インフラのテストがトートロジー・`grep -c`・WARN だけの復元・IPv4 限定照合で 3〜4 往復差し戻された | `test-lint.py` がシェル（`*.bats` / `*_test.sh` / `tests/**/*.sh`）を検査。`conventions/shell.md` を新設。`testing.md` に「ミューテーション確認」（Dev は自分の worktree、QA は統合検証で）と `test/mutation-checked`。レビュアーは結果を 1〜2 件再現 |
| 4 | REQ-021 が仕様書とテスト定義書の両方から漏れ、consistency 前に気付けなかった | `doc-validate.py` がどの `covers` にも無い REQ を WARN。spec writer のプロンプトに提出前の確認 |
| 5 | コミットの無い Dev ブランチを QA 側でマージしようとした | 統合検証の手順 0 で `git rev-list --count` と `result.commits` を確認 |
| 6 | 環境のつまずき（zsh、root 所有の `~/.cache`、ポート 80） | `agent-prompt-injection.md` の実行環境ノートの例と、作成前の確認表に追加 |
| 7 | `gh pr merge` が deny された後に再試行・判断が揺れた | `dev-flow/SKILL.md` に deny 時の扱い（同じコマンドを再試行しない、人間マージ待ちとして 1 回提示） |

## あわせて追加したもの

- **保守性のレビュー観点**（`conventions/maintainability.md`、常に注入）: 車輪の再発明をしない（`maint/reinvented-wheel`）、知識の重複（`maint/duplicated-knowledge`、認証・認可のコピーは `maint/duplicated-security-logic` で blocker）、ドメイン境界（`maint/domain-boundary`）、循環依存。これまでレビュアーの観点は「アーキテクチャ視点: 拡張性・保守コスト・依存関係」の 1 行だけで、重複は minor 扱いだった。Dev implementer には「書く前に探す」手順を、レビュアーには「実際に grep して代替を示す」手順を入れた
